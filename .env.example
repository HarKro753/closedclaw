# Required
JWT_SECRET=your-random-secret-string-here

# Optional
ADMIN_EMAILS=admin@example.com
BRAVE_API_KEY=
PORT=3900

# Auth is handled by claude-agent-sdk via ~/.claude (set by Claude Code init).
# No ANTHROPIC_API_KEY needed.
